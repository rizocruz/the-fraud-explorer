input {
 udp {
  port => 5966
  codec => plain { charset => "UTF-8" }
  type => "PrinterEvent"
 }
}

filter {
 if [type] == "PrinterEvent" {
  grok {
   break_on_match => false
   match => { 
    message => "(?m)%{TIMESTAMP_ISO8601:sourceTimestamp} %{IPORHOST:hostPrivateIP} %{WORD:computerName}\\%{WORD:userName} %{WORD:agentId} %{WORD:eventType} - a: %{GREEDYDATA:printerDevice_b64} b: %{GREEDYDATA:documentPrinted_b64}"
   }
  }
  ruby {
   code => '
	require "base64"
        require "openssl"
	   
	def Decrypt(encrypted_data, key, iv, cipher_type)
         encrypted_data.gsub!("-","+")
	 encrypted_data.gsub!("_","/")
	 encrypted_data = Base64.decode64(encrypted_data)
         encrypted_data = encrypted_data.ljust((encrypted_data.size / 16.0).ceil * 16, "\0")
	 aes = OpenSSL::Cipher::Cipher.new(cipher_type)
    	 aes.decrypt
         aes.key = key
         aes.iv = iv
         return aes.update(encrypted_data) + aes.final  
	end
        
   	event["printerDevice"] = Decrypt(event["printerDevice_b64"], "1uBu8ycVugDIJz61", "1uBu8ycVugDIJz61", "aes-128-cbc")
	event["documentPrinted"] = Decrypt(event["documentPrinted_b64"], "1uBu8ycVugDIJz61", "1uBu8ycVugDIJz61", "aes-128-cbc")
        '
  }
  mutate {
   remove_field => [ "printerDevice_b64" ]
   remove_field => [ "documentPrinted_b64" ]
  }
 }
}

output {
 if [type] == "PrinterEvent" {
  elasticsearch {
   index => "logstash-thefraudexplorer-printer-%{+YYYY.MM.dd}"
   hosts => "localhost"
  }
 }
}
